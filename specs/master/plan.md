# Implementation Plan: Landing Page

**Branch**: `landing-page-update` | **Date**: 2025-02-25 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/master/spec.md`

## Summary

Implement the Xenos landing page based on real project information (from README, Constitution, Obsidian notes). Key changes: remove fake statistics, replace with feature list; implement mobile navigation; refine UseCases to show ToWow integration; improve TrustNetworkPreview with meaningful mock data and pulse animations.

---

## Technical Context

**Language**: TypeScript 5.x (strict mode)  
**Framework**: Next.js 14 (App Router)  
**UI Library**: React 18  
**Styling**: Tailwind CSS 3.3+  
**Animation**: Framer Motion 12+  
**Authentication**: NextAuth.js (SecondMe Provider configured)  
**Deployment**: Vercel (automatic from Git push)  
**Database**: PostgreSQL (via Prisma)  
**ORM**: Prisma 6.4+

**Dependencies**:
- @noble/ed25519 (signatures)
- @digitalcredentials/vc (verifiable credentials)
- next-auth (authentication)
- framer-motion (animations)

---

## Constitution Check

**Status**: ✅ PASS

| Constitution Principle | Compliance Evidence |
|------------------------|--------------------|
| Trust is verifiable, not预设 | Animations show commitment → verification flow |
| Contextual reputation | UseCases include reputation-based scenarios |
| No fake data | Stats replaced with factual feature list |
| Lightweight | Animations optimized, no heavy 3D |
| Openness | Links to API docs and GitHub provided |

---

## Project Structure

```text
src/app/
├── page.tsx          # Landing page (this feature)
├── layout.tsx        # Root layout (existing)
├── agents/
│   └── page.tsx      # Agent lobby (existing)
├── trust/
│   └── page.tsx      # Trust network (existing)
└── api/
    └── auth/...      # NextAuth routes (existing)

specs/master/
├── spec.md           # Feature specification
├── research.md       # Research decisions
├── data-model.md     # Data structures
├── quickstart.md     # Developer guide
├── plan.md           # This file
└── contracts/        # (empty - no external contracts needed)
```

---

## Complexity Tracking

**No violations** - the project stays within single repo, single Next.js app. Landing page is a new route within existing application structure.

---

## Phase 1: Data Model & Contracts

### data-model.md

Defines:
- `CoreConcept` (icon, title, description, gradient)
- `WorkflowStep` (number, title, description)
- `UseCase` (icon, title, description, tags)
- `FeatureItem` (icon, text) - replaces fake stats
- `TrustNetworkData` (nodes, connections) - mock data for canvas

See `data-model.md` for full details.

### contracts/

No external API contracts needed for landing page (static content). TrustNetworkPreview uses mock data; future API contract TBD when backend endpoint is implemented.

### quickstart.md

Developer guide covering:
- Prerequisites (Node.js 18+, npm install)
- Running dev server (`npm run dev`)
- Making changes (edit `src/app/page.tsx`)
- Type checking (`npx tsc --noEmit`)
- Build and production test
- Deployment to Vercel
- Troubleshooting

See `quickstart.md`.

---

## Phase 2: Tasks

Tasks will be generated by `/speckit.tasks` command after plan approval.

**Preliminary task list** (from spec ACs):
1. Implement mobile hamburger menu in Navbar
2. Replace Stats section with FeaturesList component
3. Update UseCases content to match research decisions
4. Refactor TrustNetworkPreview: separate `getMockNetwork()`, add pulse animation
5. Verify all CTA links correct (`/api/auth/login`, `/trust`, `/agents`)
6. TypeScript type check (0 errors)
7. Responsive testing (mobile/tablet/desktop)
8. Performance audit (Lighthouse, 60fps animations)
9. Accessibility check (contrast, keyboard nav, reduced-motion)
10. Deploy to Vercel and test production URL

---

## Agent Context

Updated `AGENTS.md` with:
- Language: TypeScript 5.x
- Framework: Next.js 14
- Database: PostgreSQL (Prisma)
- Dependencies: framer-motion, next-auth, prisma, tailwindcss

See `AGENTS.md` for full context.
