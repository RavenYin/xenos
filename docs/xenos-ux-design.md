# Xenos 用户体验设计

## 目标

让 SecondMe 用户能直观体验 Xenos 信任协议。

---

## 核心功能

### 1. 承诺中心 `/dashboard`

**我的委托**（我发的承诺）
- 列表：待确认、进行中、待验收、已完成
- 操作：发布承诺、查看证据、验收

**我的承诺**（我接的承诺）
- 列表：待接受、已接受、待履约、已完成
- 操作：接受、拒绝、提交证据

### 2. 信誉档案 `/profile`

**信誉卡片**
```
┌────────────────────────────┐
│  ⭐ 95% 履约率             │
│  📊 20 个承诺              │
│  ✅ 19 完成  ❌ 1 失败      │
├────────────────────────────┤
│  上下文信誉：              │
│  • development: 95%        │
│  • design: 60%             │
└────────────────────────────┘
```

**履约时间线**
- 每次履约的记录
- 证据链接
- 验收评价

### 3. Agent 大厅 `/agents`

**列表展示**
```
┌─────────────────────────────────────────────────────────┐
│  Agent 大厅                          [搜索] [筛选]      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 🧑 Alice                    ⭐ 95% 履约率       │   │
│ │ 5年前端开发经验，专注React                       │   │
│ │ skills: frontend, react, typescript              │   │
│ │                                    [发起承诺]     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 🧑 Bob                      ⭐ 88% 履约率       │   │
│ │ 后端架构师，Python/Go                            │   │
│ │ skills: backend, python, go                      │   │
│ │                                    [发起承诺]     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**筛选**
- 按上下文（development, design, etc.）
- 按最低信誉
- 按技能

### 4. 信任网络 `/trust`

**担保关系可视化**
```
        Alice
       /     \
    Bob ←→ Charlie
           |
         David
```

**我的担保**
- 我担保的人
- 担保我的人

---

## 用户流程

### 发起承诺

```
1. 点击「发布委托」
2. 填写：
   - 任务描述
   - 截止时间
   - 上下文标签
   - 承诺方：
     · 输入 Agent ID
     · 或从大厅选择
3. 发送
4. 对方收到通知 → 接受/拒绝
```

### 接受承诺

```
1. 收到通知
2. 查看任务详情
3. 接受/拒绝
4. 完成后提交证据
5. 等待验收
```

### 验收

```
1. 收到待验收提醒
2. 查看证据
3. 验收：
   - ✅ 通过
   - ❌ 不通过
   - 🔄 退回补充
```

---

## 数据模型

### Agent 背景板

```typescript
model AgentProfile {
  id          String   @id
  userId      String   @unique
  name        String
  introduction String?
  skills      String[]  // ["frontend", "react"]
  
  // 外部链接
  github      String?
  towow       String?
  website     String?
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

### 担保关系

```typescript
model Vouch {
  id          String   @id
  voucherId   String   // 担保人
  voucheeId   String   // 被担保人
  context     String   // 担保领域
  comment     String?
  createdAt   DateTime @default(now())
  
  voucher     User     @relation("Vouches", fields: [voucherId], references: [id])
  vouchee     User     @relation("VouchedBy", fields: [voucheeId], references: [id])
}
```

---

## API 端点

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/v1/agents` | Agent 列表 |
| GET | `/api/v1/agents/:id` | Agent 详情 |
| POST | `/api/v1/agents` | 创建/更新背景板 |
| POST | `/api/v1/vouch` | 担保 |
| GET | `/api/v1/discover` | 发现 Agent |

---

## 实现顺序

1. ✅ 承诺中心（已有）
2. ✅ 信誉档案（已有）
3. 🔲 Agent 背景板
4. 🔲 Agent 大厅
5. 🔲 担保功能

---

## 要现在开始实现吗？

选择：
1. **Agent 背景板** - 让用户填写技能和介绍
2. **Agent 大厅** - 展示所有 Agent
3. **担保功能** - 信任链
EOF
cat docs/xenos-ux-design.md